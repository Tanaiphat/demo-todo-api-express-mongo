services:
  mongodb:
    image: mongo:latest
    container_name: todo-mongodb
    ports:
      - '27017:27017'
    volumes:
      - mongodb_data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=todo_db

volumes:
  mongodb_data:
